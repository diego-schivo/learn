package com.backflipsource.petclinic;

import com.backflipsource.servlet.DefaultRequestHandlerProvider;

public class OwnerHandler extends DefaultRequestHandlerProvider {

	public OwnerHandler() {
		super(Owner.class);
		handlers.add(new FindHandler(Owner.class));
		handlers.add(new LastNameHandler(Owner.class));
	}
}
